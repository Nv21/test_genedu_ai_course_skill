# Reproducible environment for the wikipedia-trend-insights skill.
# Range-pinned (not exact) so the skill installs across Python 3.10-3.14;
# tested against: requests 2.34.2, pandas 3.0.6, numpy 2.5.3, scipy 1.18.1,
# matplotlib 3.11.2, statsmodels 0.15.0 on Python 3.14.5 (2026-09-23).
#
# Setup (once per environment):
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# or, without a venv:
#   pip install -r requirements.txt

requests>=2.31,<3
pandas>=2.2,<4
numpy>=1.26,<3
scipy>=1.11,<2
matplotlib>=3.8,<4
statsmodels>=0.14,<1   # STL seasonal-trend decomposition
